{
    "scripts": {
      "check": "tsc",
      "build": "esbuild src/index.tsx --bundle --jsx-factory=hic --outfile=bundle/bundle.js",
      "build-min": "esbuild src/index.tsx --bundle --jsx-factory=hic --minify --outfile=bundle/bundle.js",
      "serve-dev": "http-server -p 8000 --cors",
      "serve": "http-server -p 80 --cors"
    },
    "devDependencies": {
      "esbuild": "^0.15.10",
      "typescript": "^4.8.4"
    }
  }